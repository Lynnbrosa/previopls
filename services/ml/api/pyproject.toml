[project]
name = "previopls-ml-api"
version = "1.0.0"
description = "Servidor de inferência para o motor preditivo do PrevioPLS."
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
